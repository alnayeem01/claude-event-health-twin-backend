# PostgreSQL — Render / cloud (takes precedence over PG* vars)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME

# PostgreSQL — local (used when DATABASE_URL is unset)
PGHOST=localhost
PGPORT=5432
PGUSER=utsavgohel
PGPASSWORD=
PGDATABASE=bench_pgvector

# Server
PORT=3000

# AWS Bedrock (Converse API — Claude, Nova, etc.)
# Use the same region where you enabled model access in the Bedrock console.
# Local dev: access key on an IAM user with bedrock:InvokeModel (omit on EC2/Lambda with an IAM role).
 AWS_ACCESS_KEY_ID=AKIAQMEY5U2FJ67VCDID
AWS_SECRET_ACCESS_KEY=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
AWS_REGION=eu-west-2
# BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
BEDROCK_MODEL_ID=amazon.nova-pro-v1:0

# CORS: comma-separated origins, or leave unset for permissive (dev)
# FRONTEND_ORIGIN=http://localhost:5173
